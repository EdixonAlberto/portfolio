{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "format": "prettier --write *.{vue,ts,json,md}"
  },
  "devDependencies": {
    "nuxt": "^3.3.2",
    "prettier": "^2.8.7",
    "sass": "^1.60.0"
  }
}
